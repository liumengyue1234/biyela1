# 松材线虫病检测系统 - Python 依赖

# PyTorch (基础深度学习框架)
torch>=2.0.0
torchvision>=0.15.0

# YOLO 检测 (可选，如需使用 YOLO 模型请安装)
# ultralytics>=8.0.0

# OpenCV (图像处理)
opencv-python>=4.8.0
opencv-python-headless>=4.8.0

# NumPy (数值计算)
numpy>=1.24.0

# Pillow (图像处理辅助)
Pillow>=10.0.0

# Matplotlib (可视化)
matplotlib>=3.7.0

# Scikit-learn (评估指标)
scikit-learn>=1.3.0

# tqdm (进度条)
tqdm>=4.65.0

#albumentations (数据增强)
albumentations>=1.3.1

# 建议安装 GPU 版本的 PyTorch
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
